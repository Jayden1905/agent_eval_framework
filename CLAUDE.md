## Agent skills

### Issue tracker

Issues live as GitHub issues in `Jayden1905/agent_eval_framework`. Use the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: one `CONTEXT.md` at the repo root and ADRs under `docs/adr/`. See `docs/agents/domain.md`.
